version: "3.9"

services:
  react-app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3002:80"     # 3000 local → 80 del contenedor (Nginx)
    container_name: react-app
    restart: always
